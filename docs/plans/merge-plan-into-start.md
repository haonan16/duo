# Merge /duo:plan and /duo:draft into /duo:start

## Goal Description

Consolidate the `/duo:plan` and `/duo:draft` commands into `/duo:start`. The original draft proposed removing `/duo:plan` only (8 to 7 commands). This plan expands scope to also remove the already-deprecated `/duo:draft`, bringing the CLAUDE.md command table from 8 to 7 entries (since `draft` was never listed there) and the help.md table from 10 to 8 entries. `/duo:start` already auto-detects draft vs plan input and routes to plan generation, making these separate commands redundant. The merge preserves full plan generation functionality including Codex refinement loops (currently only available via `/duo:plan`), and adds the `--max`, `--codex-model`, and `--codex-timeout` flags to `/duo:start`. These three flags are not new functionality -- they already exist on `/duo:plan` and are being carried over.

## Acceptance Criteria

Following TDD philosophy, each criterion includes positive and negative tests for deterministic verification.

- AC-1: `/duo:plan` command file is removed
  - Positive Tests (expected to PASS):
    - `commands/plan.md` no longer exists in the repository
    - No skill registration or frontmatter references the `plan` command name
  - Negative Tests (expected to FAIL):
    - Running `/duo:plan` should fail with "unknown command" or equivalent
    - `ls commands/plan.md` should return file-not-found

- AC-2: `/duo:draft` command file is removed
  - Positive Tests (expected to PASS):
    - `commands/draft.md` no longer exists in the repository
  - Negative Tests (expected to FAIL):
    - Running `/duo:draft` should fail with "unknown command" or equivalent

- AC-3: `/duo:start` supports full Codex refinement when generating plans
  - Positive Tests (expected to PASS):
    - `/duo:start draft.md --plan-only` generates a plan and runs Codex refinement by default
    - `/duo:start draft.md --plan-only --skip-review` generates a plan without Codex refinement
    - `/duo:start draft.md --plan-only --max 3` limits refinement to 3 rounds
    - `/duo:start draft.md --codex-model o4-mini:high` passes model to Codex
    - `/duo:start draft.md --codex-timeout 120` passes timeout to Codex
    - `/duo:start "inline text" --plan-only` generates plan from inline text with Codex refinement
    - `/duo:start draft.md --draft-only` works as an alias for `--plan-only` (backward compat)
  - Negative Tests (expected to FAIL):
    - `/duo:start draft.md --plan-only --max 0` should reject invalid max value or treat as skip-review
    - `/duo:start draft.md --plan-only --max -1` should reject negative max
  - AC-3.1: When `/duo:start` routes a draft to plan generation (without `--plan-only`), it uses `--skip-review` by default to avoid blocking the loop startup
    - Positive: `/duo:start draft.md` detects draft, generates plan with `--skip-review`, then starts loop
    - Negative: `/duo:start draft.md` should NOT run Codex refinement rounds before starting the loop
  - AC-3.2: When refinement flags (`--max`, `--codex-model`, `--codex-timeout`) are passed WITHOUT `--plan-only`, they imply `--plan-only` behavior -- generate plan with refinement, then stop (do not auto-start loop)
    - Positive: `/duo:start draft.md --max 3` generates plan with 3 refinement rounds and stops
    - Negative: `/duo:start draft.md --max 3` should NOT auto-start the development loop after plan generation

- AC-4: All active files with `/duo:plan` or `/duo:draft` command references are updated
  - Positive Tests (expected to PASS):
    - `grep -r "duo:plan" .` returns zero matches in active codebase files (excluding git history, this plan file, and historical plan docs in `docs/plans/`)
    - `grep -r "duo:draft" .` returns zero matches in active codebase files (same exclusions)
    - CLAUDE.md command table lists 7 commands (start, run, stop, pr, pr-stop, setup, help) -- no plan or draft
    - CLAUDE.md project rule about `commands/plan.md` template consistency is updated to reference `commands/start.md`
    - `commands/help.md` does not reference plan or draft commands
    - `docs/usage.md` shows `/duo:start` as the entry point for plan generation
    - `README.md` examples use `/duo:start` instead of `/duo:plan`
  - Negative Tests (expected to FAIL):
    - `grep -rn "duo:plan" commands/ .claude/CLAUDE.md` should return no matches (note: `duo:plan-compliance-checker` in `commands/run.md` is an agent namespace reference, NOT the command -- it must NOT be renamed)
  - AC-4.1: Historical plan docs in `docs/plans/` (e.g., `2026-03-16-ux-improvement.md`) are OUT OF SCOPE -- they are completed plans that reference `/duo:plan` as historical context and should not be modified

- AC-5: Plan generation output is identical
  - Positive Tests (expected to PASS):
    - Plan generated via `/duo:start draft.md --plan-only --skip-review` has the same structure as previously generated by `/duo:plan --input draft.md --output plan.md --skip-review`
    - Output file contains template sections (Goal Description, Acceptance Criteria, Path Boundaries, etc.)
    - Output file preserves "--- Original Design Draft Start/End ---" sections
    - Codex refinement rounds produce same review dimensions (clarity, consistency, completeness, functionality, feasibility, draft alignment)
  - Negative Tests (expected to FAIL):
    - Plan file missing any required section should be caught by existing compliance checks

- AC-6: The `duo-gen-plan` skill is updated to route through `/duo:start`
  - Positive Tests (expected to PASS):
    - `skills/duo-gen-plan/SKILL.md` references `/duo:start` for plan generation
    - Skill invocation still triggers the full gen-plan workflow
  - Negative Tests (expected to FAIL):
    - Skill referencing removed `/duo:plan` command should not work

- AC-7: Version bump in plugin.json, marketplace.json, and README.md
  - Positive Tests (expected to PASS):
    - All three files contain the same incremented version string
    - Version format is `X.Y.Z` with no suffix
  - Negative Tests (expected to FAIL):
    - Mismatched versions across the three files

## Path Boundaries

Path boundaries define the acceptable range of implementation quality and choices.

### Upper Bound (Maximum Acceptable Scope)

The implementation removes `commands/plan.md` and `commands/draft.md`, merges full Codex refinement logic (Rounds 1+ with `--max`, `--codex-model`, `--codex-timeout`) into `commands/start.md`, updates all ~10 active files that reference `duo:plan` or `duo:draft`, updates the `duo-gen-plan` skill, updates all documentation and help text, and adapts existing tests to the new entry point.

### Lower Bound (Minimum Acceptable Scope)

The implementation removes both command files, ensures `/duo:start` has the refinement flags, updates CLAUDE.md (including the project rule about template consistency), help.md, README.md, and usage.md. Minor documentation files (install-for-kimi.md) may receive simpler updates.

### Allowed Choices

- Can use: Inline plan generation logic in `commands/start.md`, or extract shared logic into a helper section referenced by start
- Can use: Flag aliasing (e.g., `--plan-only` / `--draft-only` implies plan generation mode)
- Cannot use: Introducing new command files to replace the removed ones
- Cannot use: Changing the plan output file format or template structure
- Must NOT rename: `duo:plan-compliance-checker` agent references in `commands/run.md` (this is an agent namespace, not the command)

## Feasibility Hints and Suggestions

> **Note**: This section is for reference and understanding only. These are conceptual suggestions, not prescriptive requirements.

### Conceptual Approach

1. Copy the Codex refinement loop logic (Rounds 1+) from `commands/plan.md` into `commands/start.md`, inserting it after the existing Round 0 plan generation
2. Add flag parsing for `--max`, `--codex-model`, `--codex-timeout` to the start command's argument handling section
3. When `--plan-only` is used WITHOUT `--skip-review`, execute the refinement loop after Round 0
4. When refinement flags are passed without `--plan-only`, treat as implicit `--plan-only` (generate + refine, then stop)
5. When routing draft-to-plan-to-loop (no `--plan-only`, no refinement flags), keep existing behavior: generate with `--skip-review`, then start loop
6. Delete `commands/plan.md` and `commands/draft.md`
7. Update ~10 active files with stale references (see file list below)

### Files Requiring Updates (verified via grep)

Active codebase files with `duo:plan` or `duo:draft` references:
- `.claude/CLAUDE.md` - Command table and project rule about template consistency
- `commands/help.md` - Help table
- `commands/start.md` - Cross-references to plan.md workflow (target file, will be rewritten)
- `README.md` - Usage examples
- `docs/usage.md` - Main usage documentation
- `docs/install-for-claude.md` - Installation reference
- `docs/install-for-kimi.md` - Kimi-specific docs
- `agents/draft-relevance-checker.md` - Agent description
- `tests/test-help-command.sh` - Help command test
- `skills/duo-gen-plan/SKILL.md` - Skill definition

Files explicitly OUT OF SCOPE:
- `commands/plan.md` and `commands/draft.md` (being deleted, not updated)
- `commands/run.md` - Contains `duo:plan-compliance-checker` (agent namespace, not the command)
- Historical plan docs in `docs/plans/` (completed plans, historical context)

### Relevant References

- `commands/start.md` - Current start command with inline plan generation (Phases 1-6)
- `commands/plan.md` - Codex refinement loop logic (Rounds 1+) to be merged
- `commands/draft.md` - Deprecated redirect, to be deleted
- `scripts/ask-codex.sh` - Codex interaction script used by refinement loop
- `prompt-template/codex/plan-review.md` - Review template used by refinement rounds

## Dependencies and Sequence

### Milestones

1. Merge refinement logic into start: Copy Codex refinement loop from `commands/plan.md` into `commands/start.md` and add new flag parsing
   - Phase A: Add `--max`, `--codex-model`, `--codex-timeout` flag parsing to start.md; preserve `--draft-only` alias
   - Phase B: Insert Codex refinement loop after Round 0 generation, gated by `--plan-only` without `--skip-review`
   - Phase C: Add implicit `--plan-only` behavior when refinement flags are used without `--plan-only`
   - Phase D: Verify plan generation works end-to-end through start

2. Remove obsolete commands: Delete `commands/plan.md` and `commands/draft.md`
   - Depends on: Milestone 1 complete

3. Update references: Fix all active files that reference `/duo:plan` or `/duo:draft`
   - Phase A: Core files -- CLAUDE.md (command table AND project rule about template consistency), help.md, README.md, usage.md
   - Phase B: Other active files -- agents/draft-relevance-checker.md, docs/install-for-claude.md, docs/install-for-kimi.md, skills/duo-gen-plan/SKILL.md, tests/test-help-command.sh
   - Depends on: Milestone 2 complete (so we know the final command surface)

4. Version bump and verification: Bump version in plugin.json, marketplace.json, README.md and run tests
   - Depends on: Milestone 3 complete

## Implementation Notes

### Scope Expansion from Original Draft
This plan intentionally expands beyond the original draft in two ways:
1. Also removes `/duo:draft` (already deprecated, redirect target disappearing)
2. Carries over `--max`, `--codex-model`, `--codex-timeout` flags from `/duo:plan` to `/duo:start` (these already existed, just changing entry point)

### Code Style Requirements
- Implementation code and comments must NOT contain plan-specific terminology such as "AC-", "Milestone", "Step", "Phase", or similar workflow markers
- These terms are for plan documentation only, not for the resulting codebase
- Use descriptive, domain-appropriate naming in code instead
- All content must be in English with no emoji or CJK characters per project rules

--- Original Design Draft Start ---

# Merge /duo:plan into /duo:start

## Problem

Currently `/duo:start` already auto-detects whether input is a draft or a plan (per commit ba7ff6f). This makes `/duo:plan` redundant as a separate command since `start` can route to plan generation automatically.

## Goal

Remove `/duo:plan` as a standalone command and fold its functionality entirely into `/duo:start`. This reduces the command surface from 8 to 7 commands, eliminating user confusion about which command to use when they have a draft.

## Scope

1. Remove the `commands/plan.md` command file (or merge its content into `commands/start.md`)
2. Ensure `/duo:start` handles all plan generation cases that `/duo:plan` currently handles, including the `--skip-review` flag
3. Update all references to `/duo:plan` across the codebase:
   - CLAUDE.md command table
   - Help command output
   - Documentation files
   - Tests
   - Any scripts that reference the plan command
4. Keep all plan generation logic intact -- only the entry point changes

## Constraints

- Plan generation behavior must remain identical
- The `--skip-review` flag (or equivalent) must still be accessible through `/duo:start`
- No breaking changes to the plan generation output format
- All existing tests for plan functionality must continue to pass (adapted to new entry point)

--- Original Design Draft End ---
